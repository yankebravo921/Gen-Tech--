# Technical Debt

1. **Brand Inconsistency**: The directory `src/components/anyflow` is internally inconsistent with the new "Gentech Plus" brand. Skipped renaming in v2.0 (Phase 2) to avoid path-collision risk across 15+ imports. Target for V3 cleanup.
